# coding: utf-8

"""
    Sysdig Secure Client API

    Sysdig Secure Client API.

    The version of the OpenAPI document: 1.1.1
    Contact: info@sysdig.com
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import json
import pprint
from pydantic import BaseModel, ConfigDict, Field, StrictStr, ValidationError, field_validator
from typing import Any, List, Optional
from sysdig_client.models.command import Command
from sysdig_client.models.connection import Connection
from sysdig_client.models.fileaccess import Fileaccess
from sysdig_client.models.kubernetes import Kubernetes
from pydantic import StrictStr, Field
from typing import Union, List, Set, Optional, Dict
from typing_extensions import Literal, Self

ENTRYRESPONSE_ONE_OF_SCHEMAS = ["Command", "Connection", "Fileaccess", "Kubernetes"]

class EntryResponse(BaseModel):
    """
    The response to a GET entry call 
    """
    # data type: Command
    oneof_schema_1_validator: Optional[Command] = None
    # data type: Connection
    oneof_schema_2_validator: Optional[Connection] = None
    # data type: Fileaccess
    oneof_schema_3_validator: Optional[Fileaccess] = None
    # data type: Kubernetes
    oneof_schema_4_validator: Optional[Kubernetes] = None
    actual_instance: Optional[Union[Command, Connection, Fileaccess, Kubernetes]] = None
    one_of_schemas: Set[str] = { "Command", "Connection", "Fileaccess", "Kubernetes" }

    model_config = ConfigDict(
        validate_assignment=True,
        protected_namespaces=(),
    )


    def __init__(self, *args, **kwargs) -> None:
        if args:
            if len(args) > 1:
                raise ValueError("If a position argument is used, only 1 is allowed to set `actual_instance`")
            if kwargs:
                raise ValueError("If a position argument is used, keyword arguments cannot be used.")
            super().__init__(actual_instance=args[0])
        else:
            super().__init__(**kwargs)

    @field_validator('actual_instance')
    def actual_instance_must_validate_oneof(cls, v):
        instance = EntryResponse.model_construct()
        error_messages = []
        match = 0
        # validate data type: Command
        if not isinstance(v, Command):
            error_messages.append(f"Error! Input type `{type(v)}` is not `Command`")
        else:
            match += 1
        # validate data type: Connection
        if not isinstance(v, Connection):
            error_messages.append(f"Error! Input type `{type(v)}` is not `Connection`")
        else:
            match += 1
        # validate data type: Fileaccess
        if not isinstance(v, Fileaccess):
            error_messages.append(f"Error! Input type `{type(v)}` is not `Fileaccess`")
        else:
            match += 1
        # validate data type: Kubernetes
        if not isinstance(v, Kubernetes):
            error_messages.append(f"Error! Input type `{type(v)}` is not `Kubernetes`")
        else:
            match += 1
        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when setting `actual_instance` in EntryResponse with oneOf schemas: Command, Connection, Fileaccess, Kubernetes. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when setting `actual_instance` in EntryResponse with oneOf schemas: Command, Connection, Fileaccess, Kubernetes. Details: " + ", ".join(error_messages))
        else:
            return v

    @classmethod
    def from_dict(cls, obj: Union[str, Dict[str, Any]]) -> Self:
        return cls.from_json(json.dumps(obj))

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Returns the object represented by the json string"""
        instance = cls.model_construct()
        error_messages = []
        match = 0

        # deserialize data into Command
        try:
            instance.actual_instance = Command.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into Connection
        try:
            instance.actual_instance = Connection.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into Fileaccess
        try:
            instance.actual_instance = Fileaccess.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into Kubernetes
        try:
            instance.actual_instance = Kubernetes.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))

        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when deserializing the JSON string into EntryResponse with oneOf schemas: Command, Connection, Fileaccess, Kubernetes. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when deserializing the JSON string into EntryResponse with oneOf schemas: Command, Connection, Fileaccess, Kubernetes. Details: " + ", ".join(error_messages))
        else:
            return instance

    def to_json(self) -> str:
        """Returns the JSON representation of the actual instance"""
        if self.actual_instance is None:
            return "null"

        if hasattr(self.actual_instance, "to_json") and callable(self.actual_instance.to_json):
            return self.actual_instance.to_json()
        else:
            return json.dumps(self.actual_instance)

    def to_dict(self) -> Optional[Union[Dict[str, Any], Command, Connection, Fileaccess, Kubernetes]]:
        """Returns the dict representation of the actual instance"""
        if self.actual_instance is None:
            return None

        if hasattr(self.actual_instance, "to_dict") and callable(self.actual_instance.to_dict):
            return self.actual_instance.to_dict()
        else:
            # primitive type
            return self.actual_instance

    def to_str(self) -> str:
        """Returns the string representation of the actual instance"""
        return pprint.pformat(self.model_dump())


